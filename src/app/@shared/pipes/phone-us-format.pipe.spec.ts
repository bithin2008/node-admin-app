import { PhoneUsFormatPipe } from './phone-us-format.pipe';

describe('PhoneUsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneUsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
