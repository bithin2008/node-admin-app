import { CardFormatterDirective } from './card-formatter.directive';

describe('CardFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CardFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
