import { CardExpiryDateDirective } from './card-expiry-date.directive';

describe('CardExpiryDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CardExpiryDateDirective();
    expect(directive).toBeTruthy();
  });
});
